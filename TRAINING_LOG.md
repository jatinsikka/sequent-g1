# Reach-and-grasp training log

One line per iteration: what we changed · why · how it did. Newest at bottom.

| Ver | Changed | Why | Result |
|-----|---------|-----|--------|
| v1–v4 | distance-based magnetic latch, smoothness 0.15 | first pass | 0% deterministic grasp; latched across a visible gap, slapped the tool |
| v5 | contact-based grasp + torso-lean + smoothness 0.04 | make the grab honest + let it reach | exploration grasp 0→34%, lift 0→26%; but tool levitated/tumbled & post-grasp flail |
| v5.1 | lock tool to hand pose+orientation; kill flat +500/step hold bonus; action-rate 0.12; post-grasp velocity damping; lift-dominant reward | stop the levitation and the rewarded flailing | tool held cleanly, but policy went **timid** (deterministic reach 0.20m, return 67) — global penalty killed the reach |
| v5.2 | action-rate 0.12 → 0.05 (keep targeted post-grasp damping) | timidity came from penalizing *all* motion; flail should be fought only post-grasp | reach recovered (0.146m@75k) but final eval **0/20 deterministic grasps** — policy learned to HOVER at 0.09–0.15m farming the flat +20/step ready bonus (grasping paid less per step) |
| v5.3 | ready bonus scaled by closeness (0→20 as d→0.05); calm-hold 8→30/step (> max hover income); episodes 150→250 steps for lift runway; resume v5.2 weights, 800k | hovering was rational under v5.2's reward — make approaching and holding strictly dominate parking | stopped @400k: gradient income alone didn't cross the final 4cm — deterministic still 0.09–0.21m, **0 grasps in 8 det clips** |
| v5.4 | adaptive spawn curriculum: robot starts +6cm closer (= exactly the plateau gap, so contact happens immediately), anneals back as rolling grasp rate >50%; resume v5.3 @400k | the mean policy never *experienced* grasp income — give it that experience directly, then stretch the distance | stopped @250k: **regression** (grasp 12% vs 24%; det dists worse) — closer spawn is off-distribution for resumed weights; 3rd resumed iteration chasing a moving reward = baggage |
| v5.5 | FRESH weights, current reward (contact grasp + scaled ready income + calm-hold + lift-dominant) + curriculum, 400k | 3 resumed fixes failed → stop patching: clean test of the final design without v5.2 hover habits; also the honest read is this is a sample-budget problem (CPU ~190k steps/h vs 10-100M-step baselines) → real fix is parallel sim on Azure | **BREAKTHROUGH: 40% grasp / 30% lift / 0 falls DETERMINISTIC** (20-ep eval, full task) — first nonzero det rate; grasps at step 24–32, tool held cleanly; motion decisive but not yet smooth (act_rate 0.92); left-hand-latch edge case at freeze_arm noted |
| v5.6 | v5.5 recipe (fresh weights + curriculum) scaled to Azure: 32× SubprocVecEnv on F32as_v7 (32c), 10M steps, batch 64→1024 (rollout buffer is 32× larger) | v5.5's own conclusion: sample-budget problem → first cloud run. NOTE: amo_jit/adapter_jit had cuda:0 baked into TorchScript; VM runs zip-patched _cpu variants (originals kept as *_gpu.pt) | 10M steps in ~2h (1,521 fps, 28× local). DETERMINISTIC (20 ep, final ckpt): **grasp 45% (+5 vs v5.5), lift 0% (−30, REGRESSION), 0 falls**, act_rate 0.77 (calmer). Anomaly: grasp episodes return ≈ −4,000 → post-grasp reward is hugely net-negative at this scale, so the policy grasps but refuses to lift — reward design, not sample count, is the binding constraint. Verdict: keep **v5.5** as Project-1 grasp skill; v5.6 = evidence for the "policies lie" narrative; per plan of record, no v5.7 — verifier next. |
| verifier v0 | NEW: `verifier.py` (pre/post contracts vs mjData) + `verify_policy.py` (claimed-vs-verified eval). Postcondition = latch + 5cm lift **sustained 0.5s** + stable | judge skills by measured physics, not the policy's own claim | **Caught everything.** v5.6: claimed 15%, verified 0% (incl. a 3.6 m/s smack that bounced the tool 16cm — correctly rejected). v5.5: claimed 55%, verified 0%. EXPOSED: env "lift" success = one frame above z=0.85 then instant termination — all logged lift rates were momentary, not sustained holds. Open design issue: success-termination kills the runway a sustained check needs; verification runs need a no-early-stop eval mode. Local v5.5 numbers also don't reproduce its logged 40/30 (max lift 4cm here) — eval-context drift to investigate before trusting any logged rate. |
| no-early-stop eval | env runs full horizon (success no longer terminates; falls/time still do) so LiftMonitor gets its 0.5s window | the day-1 0% was the early-termination confound, not a broken skill | **RESOLVED + green light.** Same verifier, identical eval: **v5.5 verified 55%** (claimed 60%) — a real grasp+lift-and-hold skill; **v5.6 verified 0%** (claimed 30%) — reward hack. The "eval-context drift" was this confound, not drift; v5.5 reproduces/exceeds its logged lift once measured correctly. We now have all three video assets: a verified success (v5.5), a caught failure (v5.6), and a verifier that discriminates. v5.5 is the Project-1 grasp skill of record. |
| bp-v0 (red) | **press_button, first real run.** Found the long-blamed "base falls" blocker is already FIXED (stands 200 steps); real blocker was geometric — at the old spawn the hand maxed out 0.48m SHORT of the button (height-dominated). Fix: reach-ready arm bias + spawn y−1.50→−1.56 so the button is inside the action envelope (FK 0.013m; reachable press 0.033m verified dynamically); reward reshaped depth-monotonic (killed the flat +10/step hold plateau → graze trap) + balance/recoil term + fixed active-hand bug (penalty used the frozen hand). PPO 200k, CPU, --no_wandb | the env was geometrically unsolvable before any training; make the press reachable, then reward DEPTH not a threshold-graze (avoid the grasp's brittle-success failure) | **trained on Azure (F32als_v7, 32 envs, 1474 fps, 2M steps, ~22min).** Stochastic press 8/10 BUT **deterministic = HOVER: max_depth 0.04cm, held 0 steps, base upright (z=0.743, no fall).** Same failure as grasp v5.2 — the mean policy parks near the button farming proximity/approach reward; only exploration noise stumbles into presses. Not a skill. Reachable-press margin is thin (0.033m max), so hover dominates. NEXT (bp-v1): make the press strictly dominate hovering — gate proximity/approach reward on near-contact (or cut its weight), raise press-depth weight, consider entropy anneal so deterministic≈stochastic. Probes: _reachgrid/_presssolve.py |
| bp-v1 | proximity reward CONTACT-GATED (0 beyond 10cm, ramps to contact) to kill hover income; 2M/32env Azure | bp-v0 parked farming far-field proximity | **IMPROVED but still short: deterministic max_depth 0.96cm (vs v0 0.04), held 0, upright; stochastic 8/10.** Mean now commits partway but won't clear the 2cm threshold. Best version so far. ckpt `final_button_red_v1.zip` on VM |
| bp-v2 | + steep per-step hold_bonus (30) past 2cm threshold (grasp-v5.3 analog) to pull mean across the cliff; 2M/32env | v1 mean parked at 1cm; make crossing+holding dominate | **REGRESSED: deterministic 0.04cm (hover again), stochastic 5/10 (worse than v1's 8).** The reward cliff destabilized value learning. Reverted thinking: reward tuning has hit a wall (v0 hover → v1 0.96cm → v2 hover). Likely ARCHITECTURAL: reachable press margin is thin (3.3cm max reach vs 2cm threshold), so the deterministic mean can't reliably clear it. NEXT (bp-v3): widen the margin GEOMETRICALLY (spawn ~3-5cm closer / deepen reach bias / lower button) so a comfortable press sits well inside the action envelope, then the v1 reward should give a clean deterministic press. Decision paused to Jatin. |

## Grasp rebuild (Robotiq 2F-85, fixed base, `grasp_fixed_env.py` + `train_grasp.py`)

| Ver | Changed | Why | Result |
|-----|---------|-----|--------|
| gr-v0 | FIRST run on the rebuilt env. Pure RL, NO BC / NO curriculum, fixed spawn. PPO 1.5M / 8-env SubprocVecEnv (local, 16c), ~920 fps (~33min). Reward = grip(2) + lift(6·h, gated on two-sided pad contact) + hold(1) + knock(-3). Pre-flight: `_reach_grasp.py` (tool reachable, 0cm) + `_grasp_validate3.py` (scripted pinch+lift 5.5cm) proved env solvable; corrected last session's "IK config wrong" misdiagnosis (tracking was already solved, tool reachable). | confirm the rebuilt gripper env LEARNS a real (contact-gated, non-hacked) grasp before scaling | **Det eval (20ep): grasp 90% / lift>3cm 100% / mean-best-lift 22cm / success 0%.** Env learns fast & clean — but the rollout is a **ballistic scoop**: tool flung to 22cm with FLICKERING two-sided grip (trace: grasped toggles T/F as the tool tumbles between pads), episode ends `knocked` (~41 steps). "90% grasp" = ≥1 contact frame, not a stable hold. Root cause = reward: `lift` pays 6·height UNBOUNDED so flinging-high dominates the 1/step hold bonus; policy correctly optimizes "lift as high as possible," not "lift a little and hold." Same yank pattern as old grasp-v5.1/v5.6. NEXT (gr-v1): saturate lift reward at a modest target (~10cm plateau), make sustained hold dominate, require grip-continuity before lift pays (anti-scoop), penalize tool vertical velocity post-grip. Decision paused to Jatin. Assets: `gr-v0-0623_curve.png`, `gr-v0-0623_rollout.gif`, ckpt `runs/gr-v0-0623/gr-v0-0623_final.zip`. |
| gr-v1 | anti-yank reward redesign, RESUME gr-v0 weights. lift reward SATURATED at 10cm (no pay for flinging higher), hold bonus 1→4 (dominates capped lift), anti-scoop GRIP_K=5 (lift/hold pay only after 5 continuous grip steps), + tool vertical-velocity penalty (vel·max(0,vz−0.3)) post-grip. PPO 1.5M / 8-env, ~25min. | v0 flung the tool to 22cm with flickering grip (0 hold); make a controlled lift-and-hold strictly dominate | **Det eval (20ep): grasp 100% / lift>3cm 100% / mean-best-lift 10.6cm (was 22) / success 0%.** Fling KILLED — lifts to ~8cm and HOLDS ~16 continuous steps (v0 held 0). But grip slips ~step 44 (16/25 needed for sustained-hold success), tool drops → knocked. So close. ep_rew 48→81. Trend is clearly toward a real hold; likely undertrained on the new reward (only 1.5M resumed) OR a slow deterministic drift opens the grip. NEXT (gr-v2): primary = more training (resume gr-v1, +2-3M) to extend the hold past 25; fallback = nudge hold incentive / penalize grip-loss. Decision paused to Jatin. Assets: `gr-v1-0623_curve.png`, `gr-v1-0623_rollout.gif`, ckpt `runs/gr-v1-0623/gr-v1-0623_final.zip`. |
| gr-v2 | RESUME gr-v1, same anti-yank reward, +2.5M steps (~40min, 8-env local). | gr-v1 held 16/25 — looked undertrained on the new reward, so extend. | **Det eval (20ep): grasp 100% / lift>3cm 100% / success 60% / mean-best-lift 11.4cm.** SUSTAINED-HOLD SOLVED on the pedestal — 60% of episodes hold grip+5cm for the full 0.5s (v0 0% → v1 16/25 → v2 60%). The "just train more" call was right; no reward change needed. This is a real verified grasp skill on the rebuilt Robotiq gripper. NEXT: port scene from floating pedestal → real table/machine context + re-validate (committed step, see memory). Assets: `gr-v2-0623_curve.png`, `gr-v2-0623_rollout.gif`, ckpt `runs/gr-v2-0623/gr-v2-0623_final.zip`. |
| gr-v3-robust | ROBUSTNESS TEST: tool mass 50g→250g + friction 3.0→1.0 (realistic), resume gr-v2, +2.5M. | Jatin suspected the grip was magnetic/flattered. Physics check (`_grasp_physcheck.py`) confirmed it's a REAL contact pinch (gripper closes 0→0.76, both pads 6–85N, 0 welds) — but flattered by the 50g/sticky setup. | **gr-v2 zero-shot on realistic tool: grasp 100%→45%, hold 60%→0%.** After 2.5M retrain: **grasp 75%, hold 10%, best-lift 10.1cm.** Verdict: the grasp is NOT robust — the 60% hold was propped up by the toy setup. The 250g tool slips out of the jittery grip within ~3 steps (real contact, but not firm/steady enough). Honest "manipulation is hard" result, same shape as press_button reach-limit. NEXT (Jatin's call): reward grip STABILITY (penalize tool motion relative to gripper) > more compute > mechanism/geometry > report honestly + move on. Assets: `gr-v3-robust-0623_rollout.gif`. |
